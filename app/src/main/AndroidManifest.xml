<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yang_deng.monashfriendfindertest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/monash_loading"
        android:label="Monash Friend Finder"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:label="Monash Friend Finder Main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SubscribeActivity"
            android:label="Subscribe yourself" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MapActivity"
            android:label="View the Map"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ReportsActivity"
            android:label="View the Reports"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MapActivityForFriends"
            android:label="@string/title_activity_map2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SecReportActivity"
            android:label="@string/title_activity_sec_report"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>